Content:
# Infrastructure — Nginx Configuration
# Reverse proxy configuration

server {
    listen 80;
    server_name example.com;
    
    location / {
        # BUG: proxy_pass missing after nginx update
        # proxy_pass http://localhost:3000;
    }
}
